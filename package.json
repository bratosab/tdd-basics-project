{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchAll --verbose",
    "coverage": "jest --coverage",
    "e2e": "cypress open"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "cypress": "^12.7.0",
    "vite": "^4.1.0",
    "jest": "^29.5.0"
  }
}
